import styled from 'styled-components';

/**
 * Inner container for forms
 */
 const Form = styled.form`
 display: flex;
 flex-direction: column;
 justify-content: center;
 gap: 1rem;
 width: 100%;
`;

export default Form;
