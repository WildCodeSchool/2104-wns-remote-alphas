import styled from 'styled-components';

/**
 * Build a flex container for settings sections' content
 */
const Container = styled.div`
  display: grid;
  flex-direction: column;
  flex-wrap: wrap;
  width: 100%;
  padding-left: 1.5em;
  padding-right: 1.5em;
`;

export default Container;
