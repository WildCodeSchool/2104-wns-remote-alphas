import styled from 'styled-components';

/**
 * Build a simple row
 */
const Row = styled.div`
    display: flex;
    flex-direction: row;
    align-items: center;
    gap: 1em;
`;

export default Row;
